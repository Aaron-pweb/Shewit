import 'package:flutter_riverpod/flutter_riverpod.dart';

// Placeholder provider for Cart Item Count until Phase 9
final cartItemCountProvider = Provider<int>((ref) {
  // Returns 0 for now. Will be hooked up to the actual Cart state later.
  return 0; 
});
